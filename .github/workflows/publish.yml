# Build, test, and publish release packages on version tags (v*).
# Manual run: Actions → Publish → Run workflow.

name: Publish

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  DOTNET_VERSION: "10.0.x"
  PROJECT: OscarWatch/OscarWatch.csproj

jobs:
  validate:
    name: Build and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - run: dotnet build OscarWatch.slnx -c Release

      - run: dotnet test OscarWatch.slnx -c Release --no-build

  publish:
    name: ${{ matrix.display_name }}
    needs: validate
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - rid: win-x64
            os: windows-latest
            display_name: Windows x64
            archive_ext: zip
            publish_single_file: true
            package_macos_app: false
          - rid: win-arm64
            os: windows-latest
            display_name: Windows ARM64
            archive_ext: zip
            publish_single_file: true
            package_macos_app: false
          - rid: osx-arm64
            os: macos-latest
            display_name: macOS Apple Silicon
            archive_ext: tar.gz
            publish_single_file: false
            package_macos_app: true
          - rid: osx-x64
            os: macos-latest
            display_name: macOS Intel
            archive_ext: tar.gz
            publish_single_file: false
            package_macos_app: true
          - rid: linux-x64
            os: ubuntu-latest
            display_name: Linux x64
            archive_ext: tar.gz
            publish_single_file: false
            package_macos_app: false
          - rid: linux-arm64
            os: ubuntu-latest
            display_name: Linux ARM64 (Raspberry Pi 64-bit)
            archive_ext: tar.gz
            publish_single_file: false
            package_macos_app: false

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Resolve version
        id: version
        shell: bash
        run: |
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            ver="${GITHUB_REF_NAME#v}"
            echo "version=${ver}" >> "$GITHUB_OUTPUT"
            echo "assembly_version=${ver}" >> "$GITHUB_OUTPUT"
          else
            short="${GITHUB_SHA::7}"
            echo "version=0.0.0-ci.${short}" >> "$GITHUB_OUTPUT"
            # AssemblyVersion/FileVersion must be numeric (major.minor.build.revision).
            echo "assembly_version=0.0.0.0" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $out = "${{ runner.temp }}/publish-${{ matrix.rid }}"
          $singleFile = "${{ matrix.publish_single_file }}"
          dotnet publish "${{ env.PROJECT }}" `
            -c Release `
            -r "${{ matrix.rid }}" `
            --self-contained true `
            -p:Version="${{ steps.version.outputs.version }}" `
            -p:AssemblyVersion="${{ steps.version.outputs.assembly_version }}" `
            -p:FileVersion="${{ steps.version.outputs.assembly_version }}" `
            -p:InformationalVersion="${{ steps.version.outputs.version }}" `
            -p:PublishSingleFile=$singleFile `
            -p:IncludeNativeLibrariesForSelfExtract=$singleFile `
            -o $out

      - name: Publish (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          out="${{ runner.temp }}/publish-${{ matrix.rid }}"
          dotnet publish "${{ env.PROJECT }}" \
            -c Release \
            -r "${{ matrix.rid }}" \
            --self-contained true \
            -p:Version="${{ steps.version.outputs.version }}" \
            -p:AssemblyVersion="${{ steps.version.outputs.assembly_version }}" \
            -p:FileVersion="${{ steps.version.outputs.assembly_version }}" \
            -p:InformationalVersion="${{ steps.version.outputs.version }}" \
            -o "$out"

      - name: Ad-hoc sign publish tree (macOS)
        if: matrix.package_macos_app == true
        shell: bash
        run: |
          chmod +x scripts/macos/adhoc-sign.sh scripts/macos/package-app.sh
          bash scripts/macos/adhoc-sign.sh "${{ runner.temp }}/publish-${{ matrix.rid }}"

      - name: Archive
        id: archive
        shell: pwsh
        run: |
          $src = "${{ runner.temp }}/publish-${{ matrix.rid }}"
          $version = "${{ steps.version.outputs.version }}"
          $rid = "${{ matrix.rid }}"
          if ("${{ matrix.archive_ext }}" -eq "zip") {
            $dest = "${{ runner.temp }}/OscarWatch-${version}-${rid}.zip"
            Compress-Archive -Path "$src/*" -DestinationPath $dest -Force
          } else {
            $dest = "${{ runner.temp }}/OscarWatch-${version}-${rid}.tar.gz"
            tar -czf $dest -C $src .
          }
          echo "path=$dest" >> $env:GITHUB_OUTPUT

      - name: Package OscarWatch.app zip (macOS)
        if: matrix.package_macos_app == true
        id: appzip
        shell: bash
        run: |
          version="${{ steps.version.outputs.version }}"
          rid="${{ matrix.rid }}"
          dest="${{ runner.temp }}/OscarWatch-${version}-${rid}.app.zip"
          bash scripts/macos/package-app.sh \
            "${{ runner.temp }}/publish-${{ matrix.rid }}" \
            "$version" \
            "$rid" \
            "OscarWatch/icons/OscarWatch-Icon.png" \
            "$dest"
          echo "path=$dest" >> "$GITHUB_OUTPUT"

      - name: Upload artifact
        if: matrix.package_macos_app != true
        uses: actions/upload-artifact@v4
        with:
          name: OscarWatch-${{ matrix.rid }}
          path: ${{ steps.archive.outputs.path }}
          if-no-files-found: error
          retention-days: 7

      - name: Upload artifacts (macOS folder and app)
        if: matrix.package_macos_app == true
        uses: actions/upload-artifact@v4
        with:
          name: OscarWatch-${{ matrix.rid }}
          path: |
            ${{ steps.archive.outputs.path }}
            ${{ steps.appzip.outputs.path }}
          if-no-files-found: error
          retention-days: 7

  release:
    name: GitHub Release
    needs: publish
    if: github.ref_type == 'tag'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**
          generate_release_notes: true
